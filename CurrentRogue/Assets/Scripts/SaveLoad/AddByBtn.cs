using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AddByBtn : MonoBehaviour
{
	/*
	[SerializeField]
	private Player player;

	private bool containsLevel = false;
	private bool containsHealth = false;
	private bool containsAttack = false;
	private bool containsDefense = false;

	public void AddLevelBtn ()
	{
		if (!containsLevel) {
			player.testList.Add (player.listLevel);
			Debug.Log ("Added Level");
			containsLevel = true;
		}
	}

	public void AddHealthBtn ()
	{
		if (!containsHealth) {
			player.testList.Add (player.listHealth);
			Debug.Log ("Added Health");
			containsHealth = true;
		}
	}

	public void AddAttackBtn ()
	{
		if (!containsAttack) {
			player.testList.Add (player.listAttack);
			Debug.Log ("Added Attack");
			containsAttack = true;
		}
	}

	public void AddDefenseBtn ()
	{
		if (!containsDefense) {
			player.testList.Add (player.listDefense);
			Debug.Log ("Added Defense");
			containsDefense = true;
		}
	}

	//Remove

	public void RemoveLevelBtn ()
	{
		if (containsLevel) {
			player.testList.Remove (player.listLevel);
			Debug.Log ("Removed Level");
			containsLevel = false;
		}
	}

	public void RemoveHealthBtn ()
	{
		if (containsHealth) {
			player.testList.Remove (player.listHealth);
			Debug.Log ("Removed Health");
			containsHealth = false;
		}
	}

	public void RemoveAttackBtn ()
	{
		if (containsAttack) {
			player.testList.Remove (player.listAttack);
			Debug.Log ("Removed Attack");
			containsAttack = false;
		}
	}

	public void RemoveDefenseBtn ()
	{
		if (containsDefense) {
			player.testList.Remove (player.listDefense);
			Debug.Log ("Removed Defense");
			containsDefense = false;
		}
	}
	*/
}